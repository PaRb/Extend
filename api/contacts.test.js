import { getContacts } from './contacts';

// describe('Contacts', () => {
//   it('returns an array', () => {
//     return getContacts()
//       .then(result => console.log(result))
//       .catch(e => console.log(e));
//   });
// });
